from flask import Flask, request
import os
import requests
import json

app = Flask(__name__)

BOT_TOKEN = os.environ.get('BOT_TOKEN')
CHAT_ID = os.environ.get('CHAT_ID')

@app.route('/', methods=['POST'])
def tinkoff_webhook():
    data = request.get_json()

    if not data:
        return 'No data', 400

    # 🔹 ВРЕМЕННО: отправим полный JSON в Telegram
    debug_message = f"📦 Данные от Тинькофф:\n```json\n{json.dumps(data, indent=2, ensure_ascii=False)}\n```"
    requests.post(
        f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage",
        data={'chat_id': CHAT_ID, 'text': debug_message, 'parse_mode': 'Markdown'}
    )

    # Далее обычная логика
    order_id = data.get('OrderId', 'не указан')
    amount = data.get('Amount', 0) / 100
    email = data.get('Email', 'не указан')
    status = data.get('Status', 'UNKNOWN')
    success = data.get('Success', None)

    if success is True:
        message = (
            f"✅ Оплата прошла успешно!\n"
            f"🧾 Заказ: {order_id}\n"
            f"💳 Сумма: {amount} ₽\n"
            f"📧 Email: {email}"
        )
    else:
        reason = data.get('Message') or data.get('Details') or "Причина не указана"
        message = (
            f"❌ Оплата не прошла или отменена!\n"
            f"🧾 Заказ: {order_id}\n"
            f"💳 Сумма: {amount} ₽\n"
            f"📧 Email: {email}\n"
            f"📌 Статус: {status}\n"
            f"⚠️ Причина: {reason}"
        )

    requests.post(
        f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage",
        data={'chat_id': CHAT_ID, 'text': message, 'parse_mode': 'Markdown'}
    )

    return 'OK', 200

if __name__ == "__main__":
    app.run(host='0.0.0.0', port=int(os.environ.get('PORT', 8080)))
